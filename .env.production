# 生产环境配置

# 应用标题
VITE_APP_TITLE=后台管理系统

# API 基础地址（部署时修改为实际地址，支持相对路径 /api 配合 Nginx 代理或 MSW）
VITE_API_BASE_URL=/api

# 是否开启 Mock 演示 (GitHub Pages 等演示环境建议设为 true)
VITE_ENABLE_MOCKS=true

# 认证方式：'jwt' | 'sso'
VITE_AUTH_MODE=jwt

# SSO 配置（仅 AUTH_MODE=sso 时生效）
VITE_SSO_AUTHORITY=https://sso.example.com
# SSO客户端id
VITE_SSO_CLIENT_ID=my-admin-app
# SSO 重定向回调地址
VITE_SSO_REDIRECT_URI=https://admin.example.com/sso/callback
# 权限范围
VITE_SSO_SCOPE=openid offline_access profile
